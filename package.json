{
  "name": "clinicbridge",
  "version": "0.0.1",
  "private": true,
  "description": "ClinicBridge — administrative data migration platform for small clinics (MVP skeleton).",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "backend:dev": "pnpm --filter backend dev",
    "backend:build": "pnpm --filter backend build",
    "frontend:dev": "pnpm --filter frontend dev",
    "frontend:build": "pnpm --filter frontend build",
    "test:security": "pnpm --filter backend test:security",
    "security:test": "pnpm --filter backend test:security"
  }
}
